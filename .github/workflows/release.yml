name: Release Kodi Addons

on:
  push:
    branches:
      - master

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: release-master
  cancel-in-progress: false

jobs:
  release:
    if: github.actor != 'github-actions[bot]' && !contains(github.event.head_commit.message, '[skip ci]')
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.version.outputs.tag }}
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Resolve next semantic version
        id: version
        run: |
          set -euo pipefail
          git fetch --tags --force
          version="$(python3 scripts/version.py next)"
          echo "version=${version}" >> "${GITHUB_OUTPUT}"
          echo "tag=v${version}" >> "${GITHUB_OUTPUT}"
          echo "Releasing ${version}"

      - name: Apply release version
        run: python3 scripts/version.py set "${{ steps.version.outputs.version }}"

      - name: Validate Python files
        run: |
          set -euo pipefail
          PYTHONPYCACHEPREFIX=.pycache python3 -m py_compile \
            plugin.video.pentaract/default.py \
            plugin.video.pentaract/resources/lib/api.py \
            scripts/build_repository.py \
            scripts/version.py

      - name: Build repository artifacts
        run: PYTHONPYCACHEPREFIX=.pycache python3 scripts/build_repository.py

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pentaract-kodi-${{ steps.version.outputs.tag }}
          if-no-files-found: error
          path: |
            docs/plugin.video.pentaract.zip
            docs/repository.pentaract.zip
            repository/addons.xml
            repository/addons.xml.md5
            repository/zips/**/*.zip

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Commit release version files
        id: commit
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add plugin.video.pentaract/addon.xml repository.pentaract/addon.xml

          if git diff --cached --quiet; then
            echo "created=false" >> "${GITHUB_OUTPUT}"
            echo "No tracked version files changed."
            exit 0
          fi

          git commit -m "chore(release): v${{ steps.version.outputs.version }} [skip ci]"
          echo "created=true" >> "${GITHUB_OUTPUT}"

      - name: Create tag
        run: |
          set -euo pipefail
          if git rev-parse "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.version.outputs.tag }} already exists."
            exit 0
          fi
          git tag -a "${{ steps.version.outputs.tag }}" -m "Release ${{ steps.version.outputs.tag }}"

      - name: Push release commit and tag
        run: |
          set -euo pipefail
          if [ "${{ steps.commit.outputs.created }}" = "true" ]; then
            git push origin HEAD:master
          fi
          git push origin "${{ steps.version.outputs.tag }}"

      - name: Publish GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          if gh release view "${{ steps.version.outputs.tag }}" >/dev/null 2>&1; then
            echo "GitHub release ${{ steps.version.outputs.tag }} already exists."
            exit 0
          fi

          gh release create "${{ steps.version.outputs.tag }}" \
            docs/plugin.video.pentaract.zip \
            docs/repository.pentaract.zip \
            repository/addons.xml \
            repository/addons.xml.md5 \
            repository/zips/plugin.video.pentaract/*.zip \
            repository/zips/repository.pentaract/*.zip \
            --generate-notes \
            --title "${{ steps.version.outputs.tag }}"

  deploy-pages:
    needs: release
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
